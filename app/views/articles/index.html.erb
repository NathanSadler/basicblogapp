<h1>Articles</h1>
<ul>
  <%= link_to "Create a new article", new_article_path %>

  <!-- Displays a link to each article -->
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>
