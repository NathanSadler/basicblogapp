<!-- Allows a user to edit a comment -->
<h1> Edit Comment </h1>
<%= form_with model:[@article, @comment] do |form| %>
  <div>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
